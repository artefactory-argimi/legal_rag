# %%
# Install the package (run this cell first)
get_ipython().run_line_magic(  # type: ignore[name-defined]
    "pip",
    "install --quiet --upgrade git+https://github.com/artefactory-argimi/legal_rag.git",
)
